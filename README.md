# dothier

Bash script to manage dotfiles hierarchy.

## Usage
`TODO`

## Supported types

 * `dir` - Directory
 * `ldir` - Local directory
 * `file` - File
 * `lfile` - Local file
 * `link` - (Sym)link
 * `git` - Git
 * `lgit` - Local git
